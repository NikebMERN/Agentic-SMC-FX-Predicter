# engine/__init__.py
"""SmartFlow AI prediction engine.

Valid-signal SMC + ICT analysis, per-symbol on-demand training and a
single prediction pipeline:

    from engine.pipeline import predict_symbol
    result = predict_symbol("EURUSD")
"""
